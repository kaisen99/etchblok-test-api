---
{title: create_tag, description: API Reference for app.routes.tags.create_tag, section_id: app_routes_tags_create_tag, section_type: function_ref}
---
# create_tag

Create a new tag.

    Expects JSON with ``name`` (required) and optional ``color``.

Create a new tag.

## Returns

| Type | Description |
|------|-------------|
| `object` | A JSON object containing the newly created tag's details or an error message |